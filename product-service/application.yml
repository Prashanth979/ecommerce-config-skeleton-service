spring:
  application:
    name: product-service

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver 
    url: jdbc:mysql://localhost:3306/product_service?allowPublicKeyRetrieval=true&useSSL=false
    username: root 
    password: Radeon@06
 
  jpa:
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
    hibernate: 
      ddl-auto: update